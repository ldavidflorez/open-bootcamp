package com.openbootcamptest;

public class Operators {
    public static void main(String[] args) {
        int a = 20;
        int b = 10;

        /*
        Arithmetic operators
         */
        int sum = a + b;
        int dif = a - b;
        int mul = a * b;
        int div = a / b;

        /*
        Logical, comparison and relation operators:
            - < > <= >= == !=
            - && || !
         */

        /*
        Designation operators:
            - = += -= *= /= %=
            - ++ --
         */
    }
}
